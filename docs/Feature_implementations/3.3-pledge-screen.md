# 3.3 Pledge Screen

Last updated: 2026-03-31

## Route

- `/pledge/:needId`

## Data Loading

- use route state when available
- otherwise use `GET /api/needs/{id}`

## Action

- create pledge with `POST /api/pledges`
- redirect to `/delivery/:pledgeId`
