# 3.4 Delivery View

Last updated: 2026-03-31

## Route

- `/delivery/:pledgeId`

## Data Loading

- route state
- cached session
- `GET /api/pledges/{id}`

## Map and Navigation

- current location shown as a user-location dot
- NGO destination uses NGO address first
- frontend geocodes NGO address for route accuracy
- stored `ngoLat` and `ngoLng` are fallback only
- Google Maps external navigation prefers address when available
